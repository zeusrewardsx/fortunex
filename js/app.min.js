( () => {
    var t = {
        496: function(t, e, o) {
            var n, s;
            /*! smooth-scroll v16.1.3 | (c) 2020 Chris Ferdinandi | MIT License | http://github.com/cferdinandi/smooth-scroll */
            window.Element && !Element.prototype.closest && (Element.prototype.closest = function(t) {
                var e, o = (this.document || this.ownerDocument).querySelectorAll(t), n = this;
                do {
                    for (e = o.length; 0 <= --e && o.item(e) !== n; )
                        ;
                } while (e < 0 && (n = n.parentElement));
                return n
            }
            ),
            function() {
                function t(t, e) {
                    e = e || {
                        bubbles: !1,
                        cancelable: !1,
                        detail: void 0
                    };
                    var o = document.createEvent("CustomEvent");
                    return o.initCustomEvent(t, e.bubbles, e.cancelable, e.detail),
                    o
                }
                "function" != typeof window.CustomEvent && (t.prototype = window.Event.prototype,
                window.CustomEvent = t)
            }(),
            function() {
                for (var t = 0, e = ["ms", "moz", "webkit", "o"], o = 0; o < e.length && !window.requestAnimationFrame; ++o)
                    window.requestAnimationFrame = window[e[o] + "RequestAnimationFrame"],
                    window.cancelAnimationFrame = window[e[o] + "CancelAnimationFrame"] || window[e[o] + "CancelRequestAnimationFrame"];
                window.requestAnimationFrame || (window.requestAnimationFrame = function(e, o) {
                    var n = (new Date).getTime()
                      , s = Math.max(0, 16 - (n - t))
                      , r = window.setTimeout((function() {
                        e(n + s)
                    }
                    ), s);
                    return t = n + s,
                    r
                }
                ),
                window.cancelAnimationFrame || (window.cancelAnimationFrame = function(t) {
                    clearTimeout(t)
                }
                )
            }(),
            s = void 0 !== o.g ? o.g : "undefined" != typeof window ? window : this,
            n = function() {
                return function(t) {
                    "use strict";
                    var e = {
                        ignore: "[data-scroll-ignore]",
                        header: null,
                        topOnEmptyHash: !0,
                        speed: 500,
                        speedAsDuration: !1,
                        durationMax: null,
                        durationMin: null,
                        clip: !0,
                        offset: 0,
                        easing: "easeInOutCubic",
                        customEasing: null,
                        updateURL: !0,
                        popstate: !0,
                        emitEvents: !0
                    }
                      , o = function() {
                        var t = {};
                        return Array.prototype.forEach.call(arguments, (function(e) {
                            for (var o in e) {
                                if (!e.hasOwnProperty(o))
                                    return;
                                t[o] = e[o]
                            }
                        }
                        )),
                        t
                    }
                      , n = function(t) {
                        "#" === t.charAt(0) && (t = t.substr(1));
                        for (var e, o = String(t), n = o.length, s = -1, r = "", a = o.charCodeAt(0); ++s < n; ) {
                            if (0 === (e = o.charCodeAt(s)))
                                throw new InvalidCharacterError("Invalid character: the input contains U+0000.");
                            r += 1 <= e && e <= 31 || 127 == e || 0 === s && 48 <= e && e <= 57 || 1 === s && 48 <= e && e <= 57 && 45 === a ? "\\" + e.toString(16) + " " : 128 <= e || 45 === e || 95 === e || 48 <= e && e <= 57 || 65 <= e && e <= 90 || 97 <= e && e <= 122 ? o.charAt(s) : "\\" + o.charAt(s)
                        }
                        return "#" + r
                    }
                      , s = function() {
                        return Math.max(document.body.scrollHeight, document.documentElement.scrollHeight, document.body.offsetHeight, document.documentElement.offsetHeight, document.body.clientHeight, document.documentElement.clientHeight)
                    }
                      , r = function(e) {
                        return e ? (o = e,
                        parseInt(t.getComputedStyle(o).height, 10) + e.offsetTop) : 0;
                        var o
                    }
                      , a = function(e, o, n) {
                        0 === e && document.body.focus(),
                        n || (e.focus(),
                        document.activeElement !== e && (e.setAttribute("tabindex", "-1"),
                        e.focus(),
                        e.style.outline = "none"),
                        t.scrollTo(0, o))
                    }
                      , i = function(e, o, n, s) {
                        if (o.emitEvents && "function" == typeof t.CustomEvent) {
                            var r = new CustomEvent(e,{
                                bubbles: !0,
                                detail: {
                                    anchor: n,
                                    toggle: s
                                }
                            });
                            document.dispatchEvent(r)
                        }
                    };
                    return function(l, c) {
                        var d, u, h, m, g = {
                            cancelScroll: function(t) {
                                cancelAnimationFrame(m),
                                m = null,
                                t || i("scrollCancel", d)
                            },
                            animateScroll: function(n, l, c) {
                                g.cancelScroll();
                                var u = o(d || e, c || {})
                                  , f = "[object Number]" === Object.prototype.toString.call(n)
                                  , p = f || !n.tagName ? null : n;
                                if (f || p) {
                                    var y = t.pageYOffset;
                                    u.header && !h && (h = document.querySelector(u.header));
                                    var w, v, S, _, b, L, E, q, A = r(h), x = f ? n : function(e, o, n, r) {
                                        var a = 0;
                                        if (e.offsetParent)
                                            for (; a += e.offsetTop,
                                            e = e.offsetParent; )
                                                ;
                                        return a = Math.max(a - o - n, 0),
                                        r && (a = Math.min(a, s() - t.innerHeight)),
                                        a
                                    }(p, A, parseInt("function" == typeof u.offset ? u.offset(n, l) : u.offset, 10), u.clip), T = x - y, $ = s(), C = 0, M = (w = T,
                                    S = (v = u).speedAsDuration ? v.speed : Math.abs(w / 1e3 * v.speed),
                                    v.durationMax && S > v.durationMax ? v.durationMax : v.durationMin && S < v.durationMin ? v.durationMin : parseInt(S, 10)), I = function(e) {
                                        var o, s, r;
                                        _ || (_ = e),
                                        C += e - _,
                                        L = y + T * (s = b = 1 < (b = 0 === M ? 0 : C / M) ? 1 : b,
                                        "easeInQuad" === (o = u).easing && (r = s * s),
                                        "easeOutQuad" === o.easing && (r = s * (2 - s)),
                                        "easeInOutQuad" === o.easing && (r = s < .5 ? 2 * s * s : (4 - 2 * s) * s - 1),
                                        "easeInCubic" === o.easing && (r = s * s * s),
                                        "easeOutCubic" === o.easing && (r = --s * s * s + 1),
                                        "easeInOutCubic" === o.easing && (r = s < .5 ? 4 * s * s * s : (s - 1) * (2 * s - 2) * (2 * s - 2) + 1),
                                        "easeInQuart" === o.easing && (r = s * s * s * s),
                                        "easeOutQuart" === o.easing && (r = 1 - --s * s * s * s),
                                        "easeInOutQuart" === o.easing && (r = s < .5 ? 8 * s * s * s * s : 1 - 8 * --s * s * s * s),
                                        "easeInQuint" === o.easing && (r = s * s * s * s * s),
                                        "easeOutQuint" === o.easing && (r = 1 + --s * s * s * s * s),
                                        "easeInOutQuint" === o.easing && (r = s < .5 ? 16 * s * s * s * s * s : 1 + 16 * --s * s * s * s * s),
                                        o.customEasing && (r = o.customEasing(s)),
                                        r || s),
                                        t.scrollTo(0, Math.floor(L)),
                                        function(e, o) {
                                            var s = t.pageYOffset;
                                            if (e == o || s == o || (y < o && t.innerHeight + s) >= $)
                                                return g.cancelScroll(!0),
                                                a(n, o, f),
                                                i("scrollStop", u, n, l),
                                                !(m = _ = null)
                                        }(L, x) || (m = t.requestAnimationFrame(I),
                                        _ = e)
                                    };
                                    0 === t.pageYOffset && t.scrollTo(0, 0),
                                    E = n,
                                    q = u,
                                    f || history.pushState && q.updateURL && history.pushState({
                                        smoothScroll: JSON.stringify(q),
                                        anchor: E.id
                                    }, document.title, E === document.documentElement ? "#top" : "#" + E.id),
                                    "matchMedia"in t && t.matchMedia("(prefers-reduced-motion)").matches ? a(n, Math.floor(x), !1) : (i("scrollStart", u, n, l),
                                    g.cancelScroll(!0),
                                    t.requestAnimationFrame(I))
                                }
                            }
                        }, f = function(e) {
                            if (!e.defaultPrevented && !(0 !== e.button || e.metaKey || e.ctrlKey || e.shiftKey) && "closest"in e.target && (u = e.target.closest(l)) && "a" === u.tagName.toLowerCase() && !e.target.closest(d.ignore) && u.hostname === t.location.hostname && u.pathname === t.location.pathname && /#/.test(u.href)) {
                                var o, s;
                                try {
                                    o = n(decodeURIComponent(u.hash))
                                } catch (e) {
                                    o = n(u.hash)
                                }
                                if ("#" === o) {
                                    if (!d.topOnEmptyHash)
                                        return;
                                    s = document.documentElement
                                } else
                                    s = document.querySelector(o);
                                (s = s || "#top" !== o ? s : document.documentElement) && (e.preventDefault(),
                                function(e) {
                                    if (history.replaceState && e.updateURL && !history.state) {
                                        var o = t.location.hash;
                                        o = o || "",
                                        history.replaceState({
                                            smoothScroll: JSON.stringify(e),
                                            anchor: o || t.pageYOffset
                                        }, document.title, o || t.location.href)
                                    }
                                }(d),
                                g.animateScroll(s, u))
                            }
                        }, p = function(t) {
                            if (null !== history.state && history.state.smoothScroll && history.state.smoothScroll === JSON.stringify(d)) {
                                var e = history.state.anchor;
                                "string" == typeof e && e && !(e = document.querySelector(n(history.state.anchor))) || g.animateScroll(e, null, {
                                    updateURL: !1
                                })
                            }
                        };
                        return g.destroy = function() {
                            d && (document.removeEventListener("click", f, !1),
                            t.removeEventListener("popstate", p, !1),
                            g.cancelScroll(),
                            m = h = u = d = null)
                        }
                        ,
                        function() {
                            if (!("querySelector"in document && "addEventListener"in t && "requestAnimationFrame"in t && "closest"in t.Element.prototype))
                                throw "Smooth Scroll: This browser does not support the required JavaScript methods and browser APIs.";
                            g.destroy(),
                            d = o(e, c || {}),
                            h = d.header ? document.querySelector(d.header) : null,
                            document.addEventListener("click", f, !1),
                            d.updateURL && d.popstate && t.addEventListener("popstate", p, !1)
                        }(),
                        g
                    }
                }(s)
            }
            .apply(e, []),
            void 0 === n || (t.exports = n)
        }
    }
      , e = {};
    function o(n) {
        var s = e[n];
        if (void 0 !== s)
            return s.exports;
        var r = e[n] = {
            exports: {}
        };
        return t[n].call(r.exports, r, r.exports, o),
        r.exports
    }
    o.g = function() {
        if ("object" == typeof globalThis)
            return globalThis;
        try {
            return this || new Function("return this")()
        } catch (t) {
            if ("object" == typeof window)
                return window
        }
    }(),
    ( () => {
        "use strict";
        const t = {};
        function e() {
            if (location.hash)
                return location.hash.replace("#", "")
        }
        let n = (t, e=500, o=0) => {
            t.classList.contains("_slide") || (t.classList.add("_slide"),
            t.style.transitionProperty = "height, margin, padding",
            t.style.transitionDuration = e + "ms",
            t.style.height = `${t.offsetHeight}px`,
            t.offsetHeight,
            t.style.overflow = "hidden",
            t.style.height = o ? `${o}px` : "0px",
            t.style.paddingTop = 0,
            t.style.paddingBottom = 0,
            t.style.marginTop = 0,
            t.style.marginBottom = 0,
            window.setTimeout(( () => {
                t.hidden = !o,
                !o && t.style.removeProperty("height"),
                t.style.removeProperty("padding-top"),
                t.style.removeProperty("padding-bottom"),
                t.style.removeProperty("margin-top"),
                t.style.removeProperty("margin-bottom"),
                !o && t.style.removeProperty("overflow"),
                t.style.removeProperty("transition-duration"),
                t.style.removeProperty("transition-property"),
                t.classList.remove("_slide"),
                document.dispatchEvent(new CustomEvent("slideUpDone",{
                    detail: {
                        target: t
                    }
                }))
            }
            ), e))
        }
          , s = (t, e=500, o=0) => {
            if (!t.classList.contains("_slide")) {
                t.classList.add("_slide"),
                t.hidden = !t.hidden && null,
                o && t.style.removeProperty("height");
                let n = t.offsetHeight;
                t.style.overflow = "hidden",
                t.style.height = o ? `${o}px` : "0px",
                t.style.paddingTop = 0,
                t.style.paddingBottom = 0,
                t.style.marginTop = 0,
                t.style.marginBottom = 0,
                t.offsetHeight,
                t.style.transitionProperty = "height, margin, padding",
                t.style.transitionDuration = e + "ms",
                t.style.height = n + "px",
                t.style.removeProperty("padding-top"),
                t.style.removeProperty("padding-bottom"),
                t.style.removeProperty("margin-top"),
                t.style.removeProperty("margin-bottom"),
                window.setTimeout(( () => {
                    t.style.removeProperty("height"),
                    t.style.removeProperty("overflow"),
                    t.style.removeProperty("transition-duration"),
                    t.style.removeProperty("transition-property"),
                    t.classList.remove("_slide"),
                    document.dispatchEvent(new CustomEvent("slideDownDone",{
                        detail: {
                            target: t
                        }
                    }))
                }
                ), e)
            }
        }
          , r = !0
          , a = (t=500) => {
            if (r) {
                const e = document.querySelectorAll("[data-lp]");
                setTimeout(( () => {
                    e.forEach((t => {
                        t.style.paddingRight = ""
                    }
                    )),
                    document.body.style.paddingRight = "",
                    document.documentElement.classList.remove("lock")
                }
                ), t),
                r = !1,
                setTimeout((function() {
                    r = !0
                }
                ), t)
            }
        }
          , i = (t=500) => {
            if (r) {
                const e = document.querySelectorAll("[data-lp]")
                  , o = window.innerWidth - document.body.offsetWidth + "px";
                e.forEach((t => {
                    t.style.paddingRight = o
                }
                )),
                document.body.style.paddingRight = o,
                document.documentElement.classList.add("lock"),
                r = !1,
                setTimeout((function() {
                    r = !0
                }
                ), t)
            }
        }
        ;
        function l() {
            a(),
            document.documentElement.classList.remove("menu-open")
        }
        function c(t) {
            setTimeout(( () => {
                window.FLS && console.log(t)
            }
            ), 0)
        }
        function d(t) {
            return t.filter((function(t, e, o) {
                return o.indexOf(t) === e
            }
            ))
        }
        function u(t, e) {
            const o = Array.from(t).filter((function(t, o, n) {
                if (t.dataset[e])
                    return t.dataset[e].split(",")[0]
            }
            ));
            if (o.length) {
                const t = [];
                o.forEach((o => {
                    const n = {}
                      , s = o.dataset[e].split(",");
                    n.value = s[0],
                    n.type = s[1] ? s[1].trim() : "max",
                    n.item = o,
                    t.push(n)
                }
                ));
                let n = t.map((function(t) {
                    return "(" + t.type + "-width: " + t.value + "px)," + t.value + "," + t.type
                }
                ));
                n = d(n);
                const s = [];
                if (n.length)
                    return n.forEach((e => {
                        const o = e.split(",")
                          , n = o[1]
                          , r = o[2]
                          , a = window.matchMedia(o[0])
                          , i = t.filter((function(t) {
                            if (t.value === n && t.type === r)
                                return !0
                        }
                        ));
                        s.push({
                            itemsArray: i,
                            matchMedia: a
                        })
                    }
                    )),
                    s
            }
        }
        var h = o(496);
        let m = (t, e=!1, o=500, n=0) => {
            const s = document.querySelector(t);
            if (s) {
                let r = ""
                  , a = 0;
                if (e) {
                    r = "header.header";
                    const t = document.querySelector(r);
                    t.classList.contains("_header-scroll") ? a = t.offsetHeight : (t.style.cssText = "transition-duration: 0s;",
                    t.classList.add("_header-scroll"),
                    a = t.offsetHeight,
                    t.classList.remove("_header-scroll"),
                    setTimeout(( () => {
                        t.style.cssText = ""
                    }
                    ), 0))
                }
                let i = {
                    speedAsDuration: !0,
                    speed: o,
                    header: r,
                    offset: n,
                    easing: "easeOutQuad"
                };
                if (document.documentElement.classList.contains("menu-open") && l(),
                void 0 !== h)
                    (new h).animateScroll(s, "", i);
                else {
                    let t = s.getBoundingClientRect().top + scrollY;
                    t = a ? t - a : t,
                    t = n ? t - n : t,
                    window.scrollTo({
                        top: t,
                        behavior: "smooth"
                    })
                }
                c(`[gotoBlock]: Юхуу...їдемо до ${t}`)
            } else
                c(`[gotoBlock]: Йой... Такого блоку немає на сторінці: ${t}`)
        }
        ;
        t.watcher = new class {
            constructor(t) {
                this.config = Object.assign({
                    logging: !0
                }, t),
                this.observer,
                !document.documentElement.classList.contains("watcher") && this.scrollWatcherRun()
            }
            scrollWatcherUpdate() {
                this.scrollWatcherRun()
            }
            scrollWatcherRun() {
                document.documentElement.classList.add("watcher"),
                this.scrollWatcherConstructor(document.querySelectorAll("[data-watch]"))
            }
            scrollWatcherConstructor(t) {
                if (t.length) {
                    this.scrollWatcherLogging(`Прокинувся, стежу за об'єктами (${t.length})...`),
                    d(Array.from(t).map((function(t) {
                        if ("navigator" === t.dataset.watch && !t.dataset.watchThreshold) {
                            let e;
                            t.clientHeight > 2 ? (e = window.innerHeight / 2 / (t.clientHeight - 1),
                            e > 1 && (e = 1)) : e = 1,
                            t.setAttribute("data-watch-threshold", e.toFixed(2))
                        }
                        return `${t.dataset.watchRoot ? t.dataset.watchRoot : null}|${t.dataset.watchMargin ? t.dataset.watchMargin : "0px"}|${t.dataset.watchThreshold ? t.dataset.watchThreshold : 0}`
                    }
                    ))).forEach((e => {
                        let o = e.split("|")
                          , n = {
                            root: o[0],
                            margin: o[1],
                            threshold: o[2]
                        }
                          , s = Array.from(t).filter((function(t) {
                            let e = t.dataset.watchRoot ? t.dataset.watchRoot : null
                              , o = t.dataset.watchMargin ? t.dataset.watchMargin : "0px"
                              , s = t.dataset.watchThreshold ? t.dataset.watchThreshold : 0;
                            if (String(e) === n.root && String(o) === n.margin && String(s) === n.threshold)
                                return t
                        }
                        ))
                          , r = this.getScrollWatcherConfig(n);
                        this.scrollWatcherInit(s, r)
                    }
                    ))
                } else
                    this.scrollWatcherLogging("Сплю, немає об'єктів для стеження. ZzzZZzz")
            }
            getScrollWatcherConfig(t) {
                let e = {};
                if (document.querySelector(t.root) ? e.root = document.querySelector(t.root) : "null" !== t.root && this.scrollWatcherLogging(`Эмм... батьківського об'єкта ${t.root} немає на сторінці`),
                e.rootMargin = t.margin,
                !(t.margin.indexOf("px") < 0 && t.margin.indexOf("%") < 0)) {
                    if ("prx" === t.threshold) {
                        t.threshold = [];
                        for (let e = 0; e <= 1; e += .005)
                            t.threshold.push(e)
                    } else
                        t.threshold = t.threshold.split(",");
                    return e.threshold = t.threshold,
                    e
                }
                this.scrollWatcherLogging("йой, налаштування data-watch-margin потрібно задавати в PX або %")
            }
            scrollWatcherCreate(t) {
                this.observer = new IntersectionObserver(( (t, e) => {
                    t.forEach((t => {
                        this.scrollWatcherCallback(t, e)
                    }
                    ))
                }
                ),t)
            }
            scrollWatcherInit(t, e) {
                this.scrollWatcherCreate(e),
                t.forEach((t => this.observer.observe(t)))
            }
            scrollWatcherIntersecting(t, e) {
                t.isIntersecting ? (!e.classList.contains("_watcher-view") && e.classList.add("_watcher-view"),
                this.scrollWatcherLogging(`Я бачу ${e.classList}, додав клас _watcher-view`)) : (e.classList.contains("_watcher-view") && e.classList.remove("_watcher-view"),
                this.scrollWatcherLogging(`Я не бачу ${e.classList}, прибрав клас _watcher-view`))
            }
            scrollWatcherOff(t, e) {
                e.unobserve(t),
                this.scrollWatcherLogging(`Я перестав стежити за ${t.classList}`)
            }
            scrollWatcherLogging(t) {
                this.config.logging && c(`[Спостерігач]: ${t}`)
            }
            scrollWatcherCallback(t, e) {
                const o = t.target;
                this.scrollWatcherIntersecting(t, o),
                o.hasAttribute("data-watch-once") && t.isIntersecting && this.scrollWatcherOff(o, e),
                document.dispatchEvent(new CustomEvent("watcherCallback",{
                    detail: {
                        entry: t
                    }
                }))
            }
        }
        ({});
        let g = !1;
        if (setTimeout(( () => {
            if (g) {
                let t = new Event("windowScroll");
                window.addEventListener("scroll", (function(e) {
                    document.dispatchEvent(t)
                }
                ))
            }
        }
        ), 0),
        window.innerWidth > 767.98) {
            const f = document.querySelectorAll(".data-goto-item")
              , p = -3 * window.innerHeight;
            f.forEach((t => {
                t.setAttribute("data-goto-top", p),
                t.setAttribute("data-goto-speed", "3000"),
                t.classList.contains("small") && t.setAttribute("data-goto-top", p / 3 * 2)
            }
            ));
            const y = window.innerHeight
              , w = document.querySelector(".hero__scroll-box")
              , v = document.querySelectorAll(".hero__image")
              , S = {
                title: document.querySelector(".hero__title"),
                button: document.querySelector(".hero__button-box"),
                text: document.querySelector(".hero__text"),
                bg: document.querySelector(".hero__bg"),
                logo: document.querySelector(".hero__logo img")
            }
              , _ = [{
                x: -10,
                y: -9,
                r: 30,
                s: .35
            }, {
                x: 8,
                y: -2,
                r: -10,
                s: .25
            }, {
                x: 6,
                y: 41,
                r: -20,
                s: .25
            }, {
                x: -9,
                y: 31,
                r: -20,
                s: .25
            }, {
                x: -9,
                y: -13,
                r: 30,
                s: .3
            }];
            function b() {
                const t = window.scrollY;
                if (t >= 2 * y)
                    return;
                const e = Math.min(t / y / 2 * 1.2, 1);
                1 === e && w.classList.add("fixed"),
                v.forEach(( (t, o) => {
                    const {x: n, y: s, r, s: a} = _[o];
                    t.style.transform = `translate(${n * e}vw, ${s * e}vh) rotateX(${r * e * -1}deg) scale(${1 + a * e})`
                }
                )),
                S.title.style.transform = `translateY(${-90 * e}vh)`,
                S.button.style.transform = `translateY(${40 * e}vh) `,
                S.text.style.transform = `translateY(${-75 * e}vh)`,
                S.bg.style.transform = `scale(${1 + .1 * e})`,
                S.logo.style.transform = `translateZ(${5e3 * e - 5e3}px)`
            }
            const L = document.querySelector(".page__features")
              , E = (document.querySelector(".features__scroll-box"),
            document.querySelector(".features__image img"))
              , q = document.querySelectorAll(".item-features");
            function A() {
                const t = L.getBoundingClientRect()
                  , e = L.offsetHeight;
                if (t.top > 0 || t.top < e / 4 * -3)
                    return;
                const o = Math.min(t.top / e / 3 * -4 * 1.2, 1)
                  , n = `calc(90% + 15%*${o})`;
                E.style.width = n,
                E.style.height = n;
                const s = [.1, .4, .75, .8];
                q.forEach(( (t, e) => {
                    const n = o > s[e];
                    t.classList.toggle("show", n),
                    t.classList.toggle("active-item-features", n),
                    e > 0 && q[e - 1].classList.toggle("active-item-features", !n)
                }
                ))
            }
            const x = document.querySelector(".page__stats")
              , T = (document.querySelector(".stats__scroll-box"),
            document.querySelector(".stats__bg img"))
              , $ = document.querySelectorAll(".item-taxes")
              , C = document.querySelectorAll(".item-stats");
            function M() {
                const t = x.getBoundingClientRect()
                  , e = x.offsetHeight;
                if (t.top > 0 || t.top < e / 4 * -3)
                    return;
                const o = Math.min(t.top / e / 3 * -4 * 1.2, 1)
                  , n = `calc(95% + 5%*${o})`;
                T.style.width = n,
                T.style.height = n,
                $.forEach((t => t.classList.toggle("show", o > .15)));
                const s = [.34, .48, .62, .76, .9];
                C.forEach(( (t, e) => {
                    t.classList.toggle("show", o > s[e])
                }
                ))
            }
            const I = document.querySelector(".page__how")
              , W = (document.querySelector(".how__scroll-box"),
            document.querySelector(".how__bg-main"))
              , O = document.querySelector(".how__bg-logo")
              , H = document.querySelectorAll(".item-how")
              , P = document.querySelector(".how__button");
            function R() {
                const t = I.getBoundingClientRect()
                  , e = I.offsetHeight;
                if (t.top > 0 || t.top < e / 4 * -3)
                    return;
                const o = Math.min(-t.top / (.75 * e) * 1.2, 1)
                  , n = `calc(80% + ${20 * o}%)`;
                W.style.cssText = `width: ${n}; height: ${n}; transform: translate(-50%, -50%) rotate(${30 * o - 30}deg);`,
                O.style.opacity = o,
                H.forEach((t => t.classList.toggle("show", o > .3))),
                P.classList.toggle("show", o > .6)
            }
            const F = document.querySelector(".page__faq")
              , k = (document.querySelector(".faq__scroll-box"),
            document.querySelectorAll(".spollers__item"));
            function j() {
                const t = F.getBoundingClientRect()
                  , e = F.offsetHeight;
                if (t.top > 0 || t.top < -.66 * e)
                    return;
                const o = Math.min(-t.top / (.66 * e) * 1.2, 1);
                [.2, .4, .5, .8].forEach(( (t, e) => {
                    k[e + 1].classList.toggle("show", o > t)
                }
                ))
            }
            const D = document.querySelector(".page__join")
              , B = document.querySelector(".join__scroll-box")
              , Q = document.querySelector(".join__bg-back")
              , Y = document.querySelector(".join__bg-coins")
              , U = document.querySelector(".join__text")
              , N = document.querySelector(".join__button-box")
              , z = document.querySelector(".join__bottom-left")
              , J = document.querySelector(".join__bottom-row--1")
              , Z = document.querySelector(".join__bottom-row--2")
              , K = document.querySelector(".join__copy");
            function X() {
                const t = D.getBoundingClientRect()
                  , e = D.offsetHeight;
                if (t.top > 0 || t.top < -.75 * e)
                    return;
                const o = Math.min(-t.top / (.75 * e), 1);
                B.style.top = -50 * o + "vh",
                Q.style.transform = `translate(-50%, calc(-50% + ${-5 * o}vh))`,
                Y.style.transform = `translate(-50%, calc(-50% + ${5 * o}vh))`,
                [{
                    element: U,
                    threshold: .1
                }, {
                    element: N,
                    threshold: .2
                }, {
                    element: z,
                    threshold: .35
                }, {
                    element: J,
                    threshold: .5
                }, {
                    element: Z,
                    threshold: .65
                }, {
                    element: K,
                    threshold: .9
                }].forEach(( ({element: t, threshold: e}) => {
                    t.classList.toggle("show", o > e)
                }
                ))
            }
            window.addEventListener("scroll", ( () => {
                requestAnimationFrame(b),
                requestAnimationFrame(A),
                requestAnimationFrame(M),
                requestAnimationFrame(R),
                requestAnimationFrame(j),
                requestAnimationFrame(X)
            }
            ))
        }
        window.FLS = !1,
        document.documentElement.classList.contains("loading") || window.addEventListener("load", (function() {
            setTimeout((function() {
                document.documentElement.classList.add("loaded")
            }
            ), 0)
        }
        )),
        document.querySelector(".icon-menu") && document.addEventListener("click", (function(t) {
            r && t.target.closest(".icon-menu") && (( (t=500) => {
                document.documentElement.classList.contains("lock") ? a(t) : i(t)
            }
            )(),
            document.documentElement.classList.toggle("menu-open"))
        }
        )),
        function() {
            const t = document.querySelectorAll("[data-spollers]");
            if (t.length > 0) {
                document.addEventListener("click", i);
                const e = Array.from(t).filter((function(t, e, o) {
                    return !t.dataset.spollers.split(",")[0]
                }
                ));
                e.length && r(e);
                let o = u(t, "spollers");
                function r(t, e=!1) {
                    t.forEach((t => {
                        t = e ? t.item : t,
                        e.matches || !e ? (t.classList.add("_spoller-init"),
                        a(t)) : (t.classList.remove("_spoller-init"),
                        a(t, !1))
                    }
                    ))
                }
                function a(t, e=!0) {
                    let o = t.querySelectorAll("details");
                    o.length && o.forEach((t => {
                        let o = t.querySelector("summary");
                        e ? (o.removeAttribute("tabindex"),
                        t.hasAttribute("data-open") ? (o.classList.add("_spoller-active"),
                        t.open = !0) : (t.open = !1,
                        o.nextElementSibling.hidden = !0)) : (o.setAttribute("tabindex", "-1"),
                        o.classList.remove("_spoller-active"),
                        t.open = !0,
                        o.nextElementSibling.hidden = !1)
                    }
                    ))
                }
                function i(t) {
                    const e = t.target;
                    if (e.closest("summary") && e.closest("[data-spollers]") && (t.preventDefault(),
                    e.closest("[data-spollers]").classList.contains("_spoller-init"))) {
                        const t = e.closest("summary")
                          , o = t.closest("details")
                          , r = t.closest("[data-spollers]")
                          , a = r.hasAttribute("data-one-spoller")
                          , i = o.hasAttribute("data-spoller-scroll")
                          , c = r.dataset.spollersSpeed ? parseInt(r.dataset.spollersSpeed) : 500;
                        if (!r.querySelectorAll("._slide").length && (a && !o.open && l(r),
                        o.open ? setTimeout(( () => {
                            o.open = !1
                        }
                        ), c) : o.open = !0,
                        t.classList.toggle("_spoller-active"),
                        ( (t, e=500) => {
                            t.hidden ? s(t, e) : n(t, e)
                        }
                        )(t.nextElementSibling, c),
                        i && t.classList.contains("_spoller-active"))) {
                            const t = o.dataset.spollerScroll
                              , e = +t ? +t : 0
                              , n = o.hasAttribute("data-spoller-scroll-noheader") ? document.querySelector(".header").offsetHeight : 0;
                            window.scrollTo({
                                top: o.offsetTop - (e + n),
                                behavior: "smooth"
                            })
                        }
                    }
                    if (!e.closest("[data-spollers]")) {
                        const t = document.querySelectorAll("[data-spoller-close]");
                        t.length && t.forEach((t => {
                            const e = t.closest("[data-spollers]")
                              , o = t.parentNode;
                            if (e.classList.contains("_spoller-init")) {
                                const s = e.dataset.spollersSpeed ? parseInt(e.dataset.spollersSpeed) : 500;
                                t.classList.remove("_spoller-active"),
                                n(t.nextElementSibling, s),
                                setTimeout(( () => {
                                    o.open = !1
                                }
                                ), s)
                            }
                        }
                        ))
                    }
                }
                function l(t) {
                    const e = t.querySelector("details[open]");
                    if (e && !t.querySelectorAll("._slide").length) {
                        const o = e.querySelector("summary")
                          , s = t.dataset.spollersSpeed ? parseInt(t.dataset.spollersSpeed) : 500;
                        o.classList.remove("_spoller-active"),
                        n(o.nextElementSibling, s),
                        setTimeout(( () => {
                            e.open = !1
                        }
                        ), s)
                    }
                }
                o && o.length && o.forEach((t => {
                    t.matchMedia.addEventListener("change", (function() {
                        r(t.itemsArray, t.matchMedia)
                    }
                    )),
                    r(t.itemsArray, t.matchMedia)
                }
                ))
            }
        }(),
        function() {
            function o(e) {
                if ("click" === e.type) {
                    const o = e.target;
                    if (o.closest("[data-goto]")) {
                        const n = o.closest("[data-goto]")
                          , s = n.dataset.goto ? n.dataset.goto : ""
                          , r = !!n.hasAttribute("data-goto-header")
                          , a = n.dataset.gotoSpeed ? n.dataset.gotoSpeed : 500
                          , i = n.dataset.gotoTop ? parseInt(n.dataset.gotoTop) : 0;
                        if (t.fullpage) {
                            const e = document.querySelector(`${s}`).closest("[data-fp-section]")
                              , o = e ? +e.dataset.fpId : null;
                            null !== o && (t.fullpage.switchingSection(o),
                            document.documentElement.classList.contains("menu-open") && l())
                        } else
                            m(s, r, a, i);
                        e.preventDefault()
                    }
                } else if ("watcherCallback" === e.type && e.detail) {
                    const t = e.detail.entry
                      , o = t.target;
                    if ("navigator" === o.dataset.watch) {
                        document.querySelector("[data-goto]._navigator-active");
                        let e;
                        if (o.id && document.querySelector(`[data-goto="#${o.id}"]`))
                            e = document.querySelector(`[data-goto="#${o.id}"]`);
                        else if (o.classList.length)
                            for (let t = 0; t < o.classList.length; t++) {
                                const n = o.classList[t];
                                if (document.querySelector(`[data-goto=".${n}"]`)) {
                                    e = document.querySelector(`[data-goto=".${n}"]`);
                                    break
                                }
                            }
                        t.isIntersecting ? e && e.classList.add("_navigator-active") : e && e.classList.remove("_navigator-active")
                    }
                }
            }
            if (document.addEventListener("click", o),
            document.addEventListener("watcherCallback", o),
            e()) {
                let t;
                document.querySelector(`#${e()}`) ? t = `#${e()}` : document.querySelector(`.${e()}`) && (t = `.${e()}`),
                t && m(t, !0, 500, 20)
            }
        }(),
        function() {
            g = !0;
            const t = document.querySelector("header.header")
              , e = t.hasAttribute("data-scroll-show")
              , o = t.dataset.scrollShow ? t.dataset.scrollShow : 500
              , n = t.dataset.scroll ? t.dataset.scroll : 1;
            let s, r = 0;
            document.addEventListener("windowScroll", (function(a) {
                const i = window.scrollY;
                clearTimeout(s),
                i >= n ? (!t.classList.contains("_header-scroll") && t.classList.add("_header-scroll"),
                e && (i > r ? t.classList.contains("_header-show") && t.classList.remove("_header-show") : !t.classList.contains("_header-show") && t.classList.add("_header-show"),
                s = setTimeout(( () => {
                    !t.classList.contains("_header-show") && t.classList.add("_header-show")
                }
                ), o))) : (t.classList.contains("_header-scroll") && t.classList.remove("_header-scroll"),
                e && t.classList.contains("_header-show") && t.classList.remove("_header-show")),
                r = i <= 0 ? 0 : i
            }
            ))
        }()
    }
    )()
}
)();

